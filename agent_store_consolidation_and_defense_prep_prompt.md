# 任务书:巩固与答辩准备(功能开发到此为止,这次不新增任何功能)

## 背景与判断

Phase 0-3 已经把防御性修复、RAG知识库、多步工具编排、售后闭环全部完成并验证通过。**从现在开始,项目进入功能冻结状态**,这次任务书的所有内容都是"保护已完成的东西、把材料同步到最新状态、为答辩现场做准备",不包含任何新功能。如果执行过程中发现某个点"顺手加个功能会更好",请停下来汇报,不要自行扩展范围——这是目前阶段最重要的纪律。

---

## Part A — 验证并修复 Phase 3 遗留的一致性风险(最高优先级)

### A.1 验证 after_sale_request 表的两个版本是否真的一致

现状:线上 MySQL 是通过非破坏性 ALTER 补建的 `after_sale_request` 表,`docs/schema.sql` 里是"完整外键版本"。这两者字段类型、约束、外键定义有没有完全一致,现在还没验证过——这正是 Phase 0.2 专门修复过的那类"schema.sql 和实际运行状态对不上"的问题,不能让它在 Phase 3 又重新出现。

执行:
```bash
docker compose down -v
docker compose up -d --build
```
这一步会用 `docs/schema.sql` 从空白状态完整重建数据库。重建完成后:
```sql
SHOW CREATE TABLE after_sale_request;
```
和线上非破坏性补建时实际执行的建表语句逐字段对比(类型、是否可空、默认值、外键、索引),确认完全一致。如果发现不一致,以 `docs/schema.sql` 里更完整规范的版本为准,不要为了"和之前跑通的环境保持一样"而反过来降低 schema.sql 的质量。

**这一步之后,必须在这份干净重建出来的数据库上,重新走一遍完整的售后申请流程**(申请→管理员同意/拒绝→库存和优惠券状态验证),不能只验证表结构对得上,还要验证业务逻辑在全新环境下依然正确。

### A.2 补充售后流程的测试深度

现状:售后功能加入后测试总数只从14涨到15,大概率只覆盖了一条主路径。补充以下场景的集成测试:

```
AfterSaleServiceIntegrationTest
  - 仅退款类型申请 → 管理员同意 → 订单状态变为REFUNDED,优惠券(如使用过)恢复为可用,库存不变(仅退款不涉及退货)
  - 退货退款类型申请 → 管理员同意 → 订单状态变为REFUNDED,库存正确回补,优惠券恢复可用
  - 管理员拒绝申请 → 订单状态不变,售后记录状态变为已拒绝,库存和优惠券都不受影响
  - 非法状态下申请售后应被拒绝(比如订单还在"待发货"状态就申请退货退款,如果业务逻辑上这种情况不允许的话)
  - 重复申请同一订单的售后应被正确处理(拒绝重复申请,或说明当前业务允许的行为)
```
执行 `mvn.cmd test`,确认全部通过,并报告测试总数变化。

---

## Part B — 评估并按需扩充 RAG 知识库的评价类内容

现状:77个知识块里,商品描述60条、评价只有5条、FAQ 12条。相对60个商品的规模,评价覆盖率明显偏低,如果现场演示问到"这个商品评价怎么样"这类问题,大概率命中不到评价类知识块,回答质量会打折扣。

执行:
1. 检查 `docs/seed.sql` 里 `product_review` 表当前的种子数据量,确认是不是本来评价数据就少(如果种子数据本来就只有5条评价,那知识库覆盖率低是正常的,不是构建脚本的问题)
2. 如果评价种子数据确实偏少,补充一批合理的评价内容(不需要几百条,但至少让答辩时会重点演示的几个商品——比如首页 Banner/推荐位里配置的那几个——每个都有2-3条有实际内容的评价,不是"很好用"这种空洞评价,要有具体使用场景描述,这样 RAG 检索出来的内容才有实际信息量)
3. 补充完种子数据后,重新执行 `docker compose down -v && docker compose up -d --build`(走干净重建,顺便验证 A.1 的一致性),再重新跑一次 `python scripts/build_knowledge_base.py`,确认 review 类知识块数量明显提升
4. 用几个实际问题手动验证效果提升("这款机械键盘用起来怎么样""这个显示器支架稳不稳"),对比补充前后 Agent 回答的具体程度差异

---

## Part C — 完整链路端到端回归测试(手动走一遍,不需要写自动化脚本)

这么多轮独立开发下来,还没有一次把全部功能串成一条完整用户旅程走一遍。按下面这个顺序,在浏览器里实际操作一遍(用干净重建后的环境,不要用之前测试污染过的数据状态),每一步确认符合预期再进入下一步:

```
1. 新用户注册 → 登录 → 确认 "记住我" 和 token 过期跳转行为正常
2. 浏览首页 → 确认 Banner 轮播、"猜你喜欢"推荐区、下方商品网格三者内容不重复(对应之前的版式修复)
3. 搜索一个关键词 → 确认关键词高亮、热门搜索词展示正常
4. 进入商品详情 → 确认评价展示、收藏功能正常
5. 与 AI 助手对话:
   a. 先问一个简单问题("有没有便宜的键盘"),确认走 search_products
   b. 再问一个需要 RAG 的问题("这个商品评价怎么样"/"你们退换货政策是什么"),确认走 search_knowledge_base 并且回答有具体依据
   c. 再问一个礼物规划类问题("帮我选个500元以内送女朋友的礼物"),确认走多步编排(search_products -> build_purchase_plan -> search_knowledge_base),并且最后能确认加入购物车
6. 购物车确认商品和数量正确 → 领取并使用一张优惠券 → 使用积分抵扣一部分金额 → 提交订单 → 确认金额计算正确
7. 订单列表查看该订单 → 确认状态正确
8. 对这笔订单发起售后申请(选一种类型)→ 用管理员账号登录后台 → 在售后管理页找到这条申请 → 同意 → 确认订单状态、库存、优惠券状态都正确变化
9. 管理后台依次检查:数据看板图表正常渲染、Excel订单导出可以下载且内容正确、操作日志能看到刚才管理员的售后审核操作、库存预警(如果测试环境有低库存商品)正常显示

全程记录:哪些步骤符合预期,哪些步骤发现了问题(哪怕是很小的文案/样式问题也记下来,这是目前唯一一次全链路走查,发现问题的性价比最高)。
```

发现的问题按严重程度分类处理:阻断性问题(功能报错/数据不一致)必须修复;体验性问题(文案/样式小瑕疵)视时间决定是否修复,记录下来即可,不强制在这次任务里全部解决。

---

## Part D — 同步更新开发报告和PPT内容

之前生成《项目开发报告》和《答辩PPT》的任务书是在 Phase 0-3 之前写的,现在内容需要更新:

### D.1 开发报告更新

重新打开 `docs/defense/项目开发报告.md`,在"第4章核心功能实现"里补充:
- RAG 知识库能力(数据来源、构建方式、检索工具,如实说明是"基于商品描述+评价+FAQ的轻量RAG",不要夸大)
- 多步工具编排能力(如实说明是"单一 Tool Calling Agent 的多步骤工具编排",不要写成多Agent系统)
- 售后/退款闭环

在"第5章创新点与技术亮点"里,把 RAG 和多步编排这两项作为新增的亮点补充进去,和之前已有的"AI偏好记忆驱动推荐""性能工程方法论""并发库存安全"并列展开,写清楚"为什么这么设计""怎么验证的"(可以直接引用 Part B 补充前后的效果对比、Part C 走查时的实际对话记录)。

在"第9章总结与展望"的"未完成"部分,如果售后已经做完,把这一项从"未完成"移除;如果 Part A/B 发现了新的遗留问题且时间不够修,如实补充进"未完成"或"已知问题"里,不要隐瞒。

更新"附录C答辩问答预判",补充几个新功能相关的问题,例如:
```
Q: 你们的RAG具体检索什么内容?向量存在哪里?
要点:检索范围是商品描述+用户评价+平台FAQ,规模XX条知识块;向量存储在 agent-service 自己维护的 knowledge_chunk 表(SQLAlchemy管理),不依赖专门的向量数据库,因为当前数据规模用暴力余弦相似度检索足够,没有必要引入额外的基础设施复杂度

Q: 这个"帮我选礼物"是几个Agent在协作吗?
要点:不是多Agent系统,是同一个 LangChain Tool Calling Agent,通过更完整的System Prompt设计和工具编排(先问清楚预算/对象,再依次调用search_products、build_purchase_plan、search_knowledge_base)实现的多步骤行为,底层还是一个Agent决策循环,不是多个独立服务互相调用

Q: 售后同意之后,库存和优惠券是怎么回滚的?这个操作是事务性的吗?
要点:[根据Part A.1实际实现回答,应该是在一个@Transactional方法里同时处理订单状态、库存回补(如为退货)、优惠券状态重置]
```

### D.2 PPT 更新

按之前 PPT 任务书的版式规范(冷调纸面+群青配色,不要引入新配色),在第9页("亮点①个性化推荐闭环")之后插入1-2页新亮点:
- "RAG知识库"页:用流程图展示"商品描述+评价+FAQ → 知识库 → 检索 → 回答有依据",配一个实际问答截图作为证据
- "多步工具编排"页:用时间线/流程图展示"帮我选礼物"这个场景的完整对话步骤,这一页视觉分量要给够,这是除了个性化推荐、性能工程之外第三个"别人做不到"的亮点

同步更新"已完成 vs 待改进"那一页,把售后从"待改进"移到"已完成"那一栏(如果 Part A/C 确认它已经稳定可用)。

重新走一遍之前定义的强制 QA 流程(转图片肉眼检查有没有溢出/重叠),新插入的页面尤其要仔细看一遍,不要因为是后加的页面就跳过检查。

---

## Part E — 准备并验证答辩现场演示脚本

现场演示AI功能有风险:如果评委随口问的问题没有触发预期的工具链,效果会打折扣。准备2-3个"已经验证过、保证能触发预期效果"的演示问题,不是要背台词念给评委听,而是心里有底、知道往哪个方向引导对话:

```
演示脚本示例(请用 Part C 里实际验证过的真实商品替换成具体商品名):
1. 展示RAG:"这款XX(具体商品名)用起来怎么样,评价好不好" → 预期触发 search_knowledge_base,回答里能看到具体评价内容
2. 展示多步编排:"帮我选一个XX预算以内、送给XX的礼物" → 预期Agent反问缺失信息(如果一次说全了会跳过反问,可以考虑第一次故意只说部分信息,让Agent反问,更能体现"多轮"这个特点)→ 推荐商品 → 生成购买方案 → 确认加购
3. 展示个性化推荐:提前用一个测试账号和AI聊过几句表达偏好(比如提到喜欢某个品类),退出重新登录后直接看首页"猜你喜欢"是否体现了这个偏好
```

把这几个脚本连同"预期看到什么效果"写成一份 `docs/defense/demo-script.md`,实际操作验证每一条,确保万无一失。这份文件是给人看的操作指南,不需要复杂格式,清楚列出"说什么/点什么/应该看到什么"即可。

---

## 硬性约束

1. **这次任务不新增任何业务功能**,Part A-E 全部是验证、补充测试、补充种子数据、更新文档、准备演示材料
2. Part B 补充评价种子数据时,内容要真实合理,不要为了"让演示好看"编造不符合商品实际情况的评价
3. Part D 更新报告和PPT时,严格遵守"只描述已验证实现的功能"这条一直以来的原则,RAG和多步编排的表述必须准确(轻量RAG、单Agent多步编排),不能因为想让材料更唬人就夸大成多Agent系统或全站知识库
4. Part A.1 的干净重建验证如果发现 schema.sql 和实际运行版本不一致,以 schema.sql 里更完整规范的版本为准并重新验证一遍全流程,不要为了省事而将就现状
5. 所有验证步骤(A/C/E)必须真实执行,不能只是"预期应该没问题"就跳过实际操作

## 交付要求

1. Part A.1 的一致性验证结果(一致/发现了差异及如何修复的)
2. Part A.2 新增测试后的总测试数量
3. Part B 补充前后 RAG 知识块的数量对比,以及至少一个实际问答效果对比
4. Part C 完整链路走查记录,列出所有发现的问题(不管是否修复)
5. Part D 更新后的报告/PPT 关键改动摘要
6. Part E 的 `docs/defense/demo-script.md` 完整内容,以及每条脚本实际验证的结果
