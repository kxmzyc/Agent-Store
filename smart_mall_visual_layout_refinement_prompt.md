# 任务书:视觉与排版精修——修 sticky 叠加 bug、图文错配、hero 比例、网格焦点

## 背景

这次不是性能任务,是基于两张实际截图发现的真实视觉/排版问题。请逐条核实截图里描述的现象在当前代码里确实存在,再动手,不要假设已经是这样。

---

## Part A — 修复:滚动后导航栏和分类标签栏叠在一起(优先级最高,这是功能性 bug)

在生产预览里滚动商品中心页,截图显示顶部导航("商品/购物车/收藏/订单/我的")和分类标签("数码电子/手机配件/键盘鼠标"等)在滚动后挤到了同一行、互相压字。定位分类标签栏的容器样式,确认它是否用了 `position: sticky; top: 0`(或类似),和上方已经吸顶的主导航抢占了同一个垂直位置。

修复:
```css
/* 假设主导航高度是 56px,请用实际值替换 */
.category-chips-bar {
  position: sticky;
  top: 56px; /* 主导航栏的实际高度 */
  z-index: 9; /* 比主导航低一级,不要盖过它 */
  background: var(--bg, #F5F6F8);
}
```
如果主导航高度不是固定值(比如响应式变化),改用 CSS 变量在 JS/CSS 里统一维护,不要硬编码两处不同的数字。修完后实际滚动一遍确认两层不再重叠、分类标签栏紧贴在主导航正下方吸顶。

---

## Part B — 修复:商品图文错配

"编织数据线套装"这个商品当前显示的是一张代码编辑器截图,而不是数据线的图片。请:
1. 定位商品数据源(seed 数据/数据库记录/静态 JSON,视项目实际存储方式而定),找到这条记录的图片字段
2. 顺手检查其余商品是否也存在图片和标题/描述明显不匹配的情况,不要只修这一条就停
3. 如果项目里有其他未被使用、但内容匹配的图片素材,直接替换;如果没有匹配素材,换成该品类通用的简洁占位图(不要再用明显无关的截图类图片),并在报告里列出哪些商品的图片是"替换为占位图"而不是"换成了真实匹配图",方便后续单独补真实素材

---

## Part C — Hero 文案与比例调整

### C.1 副标题重写

当前副标题:"搜索、分类、排序和库存状态都来自后端接口,前台只保留浏览与购买的关键动作。" 这句话在描述实现细节(接口/前台),和大标题"把真实商品数据,摆进一个安静的展厅。"的意境不搭。改成延续"展厅"这个比喻、强调数据真实但不点破技术实现的版本,例如:

```
没有虚构的好评，没有摆拍的库存——这里的每一件，都是它本来的样子。
```

如果这个具体措辞不合适,请保持"强调真实、不造假、留白克制"这个方向自行调整用词,不要恢复"接口""前台"这类术语。

### C.2 统计行去掉分页状态

当前 "45 件商品 · 7 个一级分类 · 第 1 页" 里的"第 1 页"是页面状态,不是用来展示"品类丰富度"的印象分数据,和前两项性质不一样。删掉这一项,只保留品类/商品数量相关的统计,分页信息交给页面下方原有的分页控件展示。

### C.3 压缩 Hero 纵向占用

当前 Hero 区域纵向内边距过大,导致首屏几乎看不到任何商品。请把 Hero 区域的上下 padding 适度收紧(建议先尝试整体减少 25%-35%),压缩后确认在常见桌面视口高度(900px 左右)下,首屏能看到第一排商品卡片的至少一部分,不需要滚动就能感知到"这是一个可以逛的商城",而不是纯落地页。

---

## Part D — 商品网格打破单调:图片比例调整 + 焦点卡片

### D.1 卡片图片纵横比

当前商品卡片图片区域偏瘦高(参考截图里的比例),调整为更接近 4:5(宽:高)的比例,不要让图片区域比商品信息区域占比过大导致卡片显得像一根长条。

### D.2 首个 HOT 商品做焦点卡片

当前四列等宽等高排布,视觉上没有焦点。给列表第一个(或者标记为最高优先级的)"HOT"商品在桌面宽视口下做跨两列的焦点展示:

```css
.product-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-card-gap, 24px);
}

.product-card--featured {
  grid-column: span 2;
  grid-row: span 2;
}

.product-card--featured .cover-frame {
  aspect-ratio: 1 / 1; /* 焦点卡片给更方正的比例,避免跨列后被拉得过高 */
}
```
只对第一页/第一个符合条件的商品应用 `product-card--featured`,不要让所有 HOT 商品都变成焦点卡片,否则又会变回均质网格,起不到打破单调的作用。移动端窄视口下焦点卡片改回和普通卡片一致的 `grid-column: span 1`,不要在小屏幕上保留跨列(小屏幕本来空间就不够,跨列会显得拥挤)。

---

## 硬性约束
1. Part A 是功能性 bug,必须最先修,并且修完后要实际滚动验证不再重叠
2. Part B 请如实报告哪些商品图片是"真实匹配替换"、哪些只是"占位图替换",不要含糊带过
3. Part C/D 的调整不影响现有的滚动联动、TransitionGroup、加购等已经跑通的动效逻辑
4. 不改变业务逻辑、接口调用、store 结构
5. 响应式:所有改动在移动端宽度(375px)下重新检查一遍,尤其是 Part D 的焦点卡片跨列逻辑

## 交付要求
1. Part A 修复前后的滚动截图对比(或明确文字描述吸顶后两层是否还重叠)
2. Part B 的商品图片核查结果列表(哪些改了、改成了什么、哪些是占位图)
3. Part C/D 的改动截图,包括桌面宽视口和 375px 窄视口各一张
