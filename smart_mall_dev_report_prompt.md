# 任务书:生成 Agent Store 项目开发报告(对齐答辩六大核心评分项)

## 背景

7月14日小组答辩,核心评分项(6大核心):项目完整度、功能完整度、创新或技术突破度、个人答辩回答评分、项目PPT、答辩后的项目开发报告及相关材料完整度。7月18日晚上前需提交项目代码、答辩PPT、项目开发报告等材料。这次任务是生成**项目开发报告**,要求内容真实、结构对齐评分项、格式专业。

## 评分项与报告章节对照(内部规划用,不需要出现在最终文档里)

| 评分项 | 对应章节 |
|---|---|
| 项目完整度 | 第1章项目概述、第2章需求分析、第7章部署工程化、第9章总结 |
| 功能完整度 | 第3章系统设计、第4章核心功能实现、第6章测试验证 |
| 创新或技术突破度 | 第5章创新点与技术亮点(重点章节)、第8章问题与解决方案 |
| 个人答辩回答评分 | 第8章(问题排查叙事本身就是最好的答辩素材)、附录C问答预判 |
| 项目PPT | 不直接产出,但 Step 6 会同步更新 ppt-outline.md 呼应报告结构 |
| 答辩后材料完整度 | 全文 + 附录,确保和代码/PPT 内容一致不矛盾 |

---

## Step 0 — 最高优先级:先找官方模板,没有就先停下来问

在动手写任何内容之前,先检查:
1. 项目仓库里(`docs/`、`docs/defense/`、根目录)有没有已下载的"项目开发报告"官方模板文件(docx/doc/pdf 都可能)
2. 检查群公告提到的"群文档"是否有本地同步路径(比如企业微信/钉钉/飞书的本地缓存目录,如果找不到就算了,不用主动联网找)

**如果找到模板**:解析它的章节顺序、标题命名、字体字号要求(通常是宋体/黑体三号标题这类学校规定),后续 Step 2 的内容要套进这个模板结构里,不要用本任务书下面给的默认结构覆盖模板要求——模板的章节名称和顺序优先级高于下面的默认大纲。

**如果没找到模板**:在报告最前面明确标注"未检测到官方模板,以下结构为通用项目开发报告结构,如学校模板已下发,请对照替换章节标题",然后使用下面 Step 2 的默认结构继续。不要假装找到了模板然后编一个自己认为像官方的格式。

---

## Step 1 — 全面核实项目真实现状(学术诚信要求,不允许编造)

这是最容易出问题的一步。本项目在此之前经历了多轮功能扩充(优惠券/积分/评论/ECharts仪表盘/Excel导出/库存预警/用户反馈/登录体验强化/个性化推荐/浏览行为上报/购物车角标/收货地址管理/推荐位配置/搜索热词/商品标签/操作日志等),**但这些功能是否真的都完整跑通、有没有部分只做了一半,现在需要重新核实,不能直接假设都已完成**。

请执行:
```bash
git log --oneline --all | head -100
git diff --stat <最早的提交>..HEAD
```
逐个检查上述功能列表里每一项:
- 数据库表是否存在(查 `docs/seed.sql` 或直连数据库 `SHOW TABLES`)
- 后端接口是否真的实现且可调用(查 Controller 代码,或用 curl 实际打一次)
- 前端页面是否真的接入且能操作(实际在浏览器里点一遍)

产出一张**现状核查表**(已实现 / 部分实现 / 未实现,附一句话说明),这张表是后续写报告内容的唯一依据——**报告里任何一句功能描述,都必须能在这张核查表里找到对应的"已实现"或"部分实现"状态,不允许写"未实现"的功能当作已完成来描述**。如果某功能只做了后端没做前端,报告里就如实写"后端接口已实现,前端界面待完善",不要含糊带过。

---

## Step 2 — 报告默认结构(如果 Step 0 没找到模板,按这个写)

### 封面 + 目录 + 小组信息
项目名称、小组编号、组员名单与分工(如果仓库里有分工文档就引用,没有就留明确的 `[待填写:组员姓名与分工]` 占位,不要虚构组员名字)。

### 第1章 项目概述
- 项目背景与选题动机(教学实训项目,对标电商+AI导购的完整业务闭环)
- 项目定位:明确写"教学实训项目,重点是功能闭环、工程结构、Docker部署和Agent工具调用,不追求生产级高并发架构"(直接引用项目 README 的原话,这是诚实且专业的定位声明)
- 核心特色一句话总结:AI 长期偏好记忆驱动的个性化推荐 + 高刷新率动效工程 + 并发安全的库存扣减

### 第2章 需求分析
- 功能性需求清单(用表格,来自 Step 1 核查表,标注实现状态)
- 非功能性需求(性能、安全、可部署性)

### 第3章 系统设计
- 总体架构图(前端 Nginx / 后端 Spring Boot / Agent FastAPI / MySQL 三层调用关系,复用/更新 `docs/architecture.md` 里已有的图)
- 技术选型理由(为什么是 Vue3+SpringBoot+FastAPI 这套组合,而不是全栈用一种语言)
- 数据库 ER 设计:复用 `docs/er-diagram.md`,**必须核对是否已经把这几轮新增的表补充进去**(coupon/user_coupon/point_record/product_review/shipping_address/banner_slot/search_keyword_log/product_tag/product_tag_relation/admin_operation_log/product_view_log),如果 ER 图文档没更新,先更新它再引用
- 模块划分与团队分工

### 第4章 核心功能实现(功能完整度重点章节)
逐模块展开,每个模块配一段实现说明 + 关键代码片段(不要贴整个文件,贴能体现设计思路的核心片段)+ 效果截图(见 Step 3):
- 用户认证(JWT + 刷新 + 记住我 + 路由守卫)
- 商品与搜索(分类/关键词/排序分页 + 搜索热词)
- 购物车与订单(含并发扣库存的事务+乐观锁机制,这段要写详细,是本项目工程亮点之一)
- AI 导购助手(工具调用架构 + 短期会话记忆 + 长期偏好记忆读写分离设计)
- 优惠券与积分体系
- 商品评论与评分
- 收货地址管理
- 管理后台(ECharts仪表盘 + Excel导出 + 操作日志 + 推荐位配置 + 标签管理)
- 个性化推荐("猜你喜欢")

### 第5章 创新点与技术亮点(创新或技术突破度重点章节,这章要写得比其他组更有深度)

这一章不要写成"功能列表2.0",要挑真正有技术含量的点讲清楚**为什么难、怎么解决的、验证结果是什么**:

1. **AI 长期偏好记忆驱动的个性化推荐闭环**:讲清楚 Agent 服务如何维护 `user_preference` 表(和业务库分离,由 Agent 服务直接读写)、前端首页推荐接口如何消费这份记忆数据、以及"用户跟AI聊天时透露的偏好会反映在首页推荐里"这个数据闭环——这是本项目结构性差异化优势,大部分同类项目的"猜你喜欢"只是销量榜单包装,这个是真正利用了对话历史的推荐

2. **高刷新率动效工程方法论**:如果 Step 1 核查确认相关性能优化工作已完成,这里要把完整的排查方法论写出来:从最初的滚动联动导致掉帧,到自建页面内性能仪表盘(FPS/LongTask/CLS 实时监测,不依赖 DevTools)、连接真实 Chrome CDP 而非 headless 环境获取可信数据、逐层隔离测试(Tier 0-4)定位真正瓶颈(合成层膨胀、全屏 filter 滤镜、Vue响应式驱动动画等反模式)、最终达成 120fps 稳定运行同时保留完整动效。这是一段完整的性能工程案例,比"我们用了CSS动画"这种描述有深度得多

3. **并发库存安全机制**:事务 + 乐观锁 + 并发验收脚本(`concurrent_order_test.ps1`),说明为什么普通的"查询-判断-扣减"会有超卖风险,乐观锁怎么解决

4. **AI Agent 工具调用架构**:`search_products`/`query_order_status` 工具设计、Agent 不直接写业务表只通过后端 API 读取业务数据的边界设计、离线兜底逻辑(无 API Key 时仍能演示工具调用链路)

### 第6章 系统测试与验证(功能完整度 + 项目完整度)
- 后端单元测试(JUnit5)结果
- 并发扣库存验收脚本实际运行结果(截图或日志)
- Agent 烟测脚本的4项验证结果(工具调用/订单查询/长期记忆/拒答非购物问题)
- 前端构建通过记录
- 如果 Step 1 确认真实浏览器性能矩阵数据存在,把关键场景的 FPS/LongTask 数据整理成表格放进来,这是少有的"用真实测量数据说话"的证据,比截图更有说服力

### 第7章 部署与工程化
Docker Compose 一键部署、环境变量管理、日志持久化、Vite代理/Nginx反向代理配置

### 第8章 开发过程中遇到的问题与解决方案(个人答辩素材重点章节)
挑 2-3 个真实、有技术深度的问题,用"问题现象 → 排查过程 → 根因定位 → 解决方案 → 验证结果"的结构展开。优先选:
- 动效导致卡顿的排查(如果 Step 1 确认这段经历属实且已解决)
- 并发扣库存的踩坑
- 其他 Step 1 核查中发现的真实技术难点

这一章刻意写成"过程叙事"而不是"结果罗列",是因为个人答辩被问到"你们怎么解决XX问题的"时,能直接照着这个结构口头复述,比临时现想要靠谱得多。

### 第9章 项目总结与展望
- 已实现功能总结(引用 Step 1 核查表)
- 未完成/待改进:如实列出(库存不足校验、SKU级库存、多商品评价、支付金额字段规范等),**不要为了显得完美而隐瞒未完成项**,如实说明反而显得团队认知清晰、答辩时被问到也不会露怯
- 个人/团队收获

### 附录
- 附录A:核心接口清单(引用 `docs/api-spec.md`)
- 附录B:数据库表结构总览(更新后的完整表清单)
- 附录C:答辩问答预判(见 Step 5)
- 附录D:分工说明

---

## Step 3 — 配图:用已有的 Playwright/CDP 基建截真实图,不要用占位图

项目里已经有 `scripts/perf-real-browser.mjs` 这套连接真实 Chrome CDP 的基建。复用同样的连接方式,写一个简单的截图脚本,对首页、商品详情页、购物车、结算页、订单列表、管理员仪表盘各截一张图,存进 `docs/defense/screenshots/`,报告里引用这些真实截图,不要用文字描述代替图片、也不要用网上找的无关图片凑数。

如果 Step 0 之前的真实 Chrome 调试实例(`--remote-debugging-port=9222`)当前没有开着,在报告里明确注明"配图待人工用真实 Chrome 截图后补充",并给出需要截哪几张图的清单,不要跳过这一步不提。

---

## Step 4 — 文档生成技术方案

优先方案:用 **pandoc** 将 Markdown 内容转换为 docx,并用 `--reference-doc` 指向 Step 0 找到的官方模板(如果模板本身是 docx,直接当 reference-doc 用,能自动继承标题样式、字体、页边距):
```bash
which pandoc || (apt-get install -y pandoc 2>/dev/null || echo "需要人工安装 pandoc")
pandoc report.md -o "docs/defense/项目开发报告.docx" --reference-doc="<模板路径>" --toc
```
如果没有可用的模板文件,不带 `--reference-doc` 参数直接转换,但确保标题层级(`#`/`##`/`###`)在 Markdown 里用得规范,转出来的 docx 目录结构才会正确。

如果 pandoc 不可用且无法安装,退化方案:用 `python-docx` 直接生成,手动设置标题样式(标题1/标题2对应章节层级,正文用宋体小四号——这是常见的学校论文格式惯例,如果 Step 0 找到模板有明确字体要求以模板为准)。

---

## Step 5 — 附录C:答辩问答预判(服务"个人答辩回答评分")

基于报告第4/5/8章的内容,列出评委(企业导师+校内导师各占50%权重,可能分别关注实现细节和技术深度)可能会问的问题,每题给"要点提示"而不是逐字答案(逐字背诵容易被追问打乱,要点提示更适合现场组织语言):

示例格式:
```
Q: 为什么用乐观锁而不是悲观锁做库存扣减?
要点:悲观锁在高并发下会造成锁等待、性能下降;乐观锁通过版本号/CAS方式,冲突时重试或返回失败,适合读多写少、冲突概率不高的场景;本项目用并发测试脚本验证了正确性
```
至少覆盖 10-15 个问题,分布在:系统设计合理性、并发安全、AI Agent 架构边界、性能优化方法论、为什么做了A功能而不是B功能这类"设计取舍"问题。

---

## Step 6 — 同步更新 docs/ppt-outline.md(呼应"项目PPT"评分项,非本次主产出)

检查现有 `docs/ppt-outline.md` 的章节结构是否和新报告的章节大致对应(不需要完全一致,PPT 通常更精简),如果差异很大,更新大纲让两者叙事逻辑一致,避免 PPT 和报告"各说各话"给评委割裂感。这一步是轻量同步,不需要产出实际 PPT 文件。

---

## 硬性约束
1. **不允许编造任何未实现的功能描述**,一切内容以 Step 1 核查表为准
2. **模板优先原则**:如果 Step 0 找到官方模板,章节结构/标题命名以模板为准,本任务书的默认大纲仅在无模板时使用
3. 配图必须是项目真实截图,不使用占位图/网络图片
4. 第9章的"未完成"部分必须如实列出,不能省略
5. 最终文件输出到 `docs/defense/项目开发报告.docx`,同时保留生成用的 Markdown 源文件在 `docs/defense/项目开发报告.md`,方便后续修改不需要重新走一遍 pandoc 流程时对照

## 交付要求
1. Step 0 的模板核查结果(找到了/没找到,如果找到,模板路径和关键格式要求)
2. Step 1 的完整现状核查表
3. 最终 docx 文件路径,以及用了 pandoc 还是 python-docx
4. Step 3 的截图清单及存放路径
5. 附录C 问答预判的完整内容
6. 明确说明报告里哪些内容是"直接复用现有 docs/ 文档",哪些是"这次新写的"
